# 📏 Aplicação com Integração do GOOGLE GEMINI

Este projeto permite enviar fotos em base64 de medidores, como hidrômetros e gasômetros, para realizar a leitura do valor em metros cúbicos (m³) utilizando a API do Google Gemini. Após a leitura, você pode confirmar se o valor lido pela IA está correto.

## 📸 Funcionalidades

- **Envio de imagem:** Envie uma foto em base64 de qualquer medidor de metros.
- **Processamento via API:** A API integrada com o Google Gemini irá interpretar a imagem e retornar o valor lido em **m³**.
- **Validação do valor:** Confirme se o valor lido pela IA está correto.

## 🚀 Como rodar o projeto

### 1. Clone o repositório

```bash
git clone https://github.com/seu-usuario/seu-repositorio.git
